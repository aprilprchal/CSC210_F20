class loopBR1 {
	public static void main(String[] args) {
        // test allPositive(int[] A)
        
        // test count(x,int[] A)

        // test average(int[] A)

        // test product(int[] A)

        // test logFloor(base,op)

        // test fibonacci(n)
    }

    /* IDENTIFY ROLES
        At keyboard:
        Leading discussion:
        if 3, writing test cases in main:
    */    
    public static boolean allPositive(int[] A) {
        // use a WHILE loop.
        // if every element is >0, return true, else false.
        return false;
    }

    /* IDENTIFY ROLES
        At keyboard:
        Leading discussion:
        if 3, writing test cases in main:
    */    
    public static int count(int x,int[] A) {
        // return the count of how many x values are in A
        return -1;
    }

    /* IDENTIFY ROLES
        At keyboard:
        Leading discussion:
        if 3, writing test cases in main:
    */    
    public static double average(int[] A) {
        // return the average of all numbers in the array
        return 0;
    }

    /* IDENTIFY ROLES
        At keyboard:
        Leading discussion:
        if 3, writing test cases in main:
    */    
    public static int product(int n1, int n2) {
        // n1*n2 = n1+n1+n1  ... n2 times
        // you cannot use the * operator of java
        // careful, one or both of the numbers can be negative
        return -1;
    }

    /* IDENTIFY ROLES
        At keyboard:
        Leading discussion:
        if 3, writing test cases in main:
    */    
    public static int logFloor(int base, int op) {
        // floor( log_base op) = b*b*b ... until ...
        // you cannot use any operator or function other than +,-,*
        // op must be positive
        return -1;
    }

    /* IDENTIFY ROLES
        At keyboard:
        Leading discussion:
        if 3, writing test cases in main:
    */    
    public static int fibonacci(int n) {
        // return the nth fibonacci number
        // fib(0) is 0. fib(1) is 1. fib(n) = fib(n-1)+fib(n-2)
        // n must be >= 0
        return -1;
    }
}
