class loopBR1 {
	public static void main(String[] args) {
        // test find(x,int[] A)
        
        // test countNegative(int[] A)

        // test sum(int[] A)

        // test power(b,e)

        // test mod(n,m);

        // test fibonacci(n)
    }
    
    /* IDENTIFY ROLES
        At keyboard:
        Leading discussion:
        if 3, writing test cases in main:
    */
    public static int find(int x, int[] A) {
        // use a WHILE loop
        // return the index of the first occurrence of value x.
        // if x is not in array, return -1
        return 0;
    }

     /* IDENTIFY ROLES
        At keyboard:
        Leading discussion:
        if 3, writing test cases in main:
    */
    public static int countNegative(int[] A) {
        // return the number of negative numbers in the array
        return -1;
    }

    /* IDENTIFY ROLES
        At keyboard:
        Leading discussion:
        if 3, writing test cases in main:
    */
    public static int sum(int[] A) {
        // return the cumulative sum of all numbers in the array
        return -1;
    }

    /* IDENTIFY ROLES
        At keyboard:
        Leading discussion:
        if 3, writing test cases in main:
    */
    public static int power(int b, int e) {
        // b^e = b*b*b ... (e times). e must be >= 0
        // you cannot use the power function of java
        return -1;
    }

    /* IDENTIFY ROLES
        At keyboard:
        Leading discussion:
        if 3, writing test cases in main:
    */    
    public static int mod(int n,int m) {
        // n % m : solve using addition or subtraction
        // n and m must both be positive
        return -1;
    }

    /* IDENTIFY ROLES
        At keyboard:
        Leading discussion:
        if 3, writing test cases in main:
    */
    public static int fibonacci(int n) {
        // return the nth fibonacci number
        // fib(0) is 0. fib(1) is 1. fib(n) = fib(n-1)+fib(n-2)
        // n must be >= 0
        return -1;
    }
}
