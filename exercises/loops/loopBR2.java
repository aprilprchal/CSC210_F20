class loopBR1 {
	public static void main(String[] args) {
        // test orderedUp(x,int[] A)
        
        // test max(int[] A)

        // test product(x,y)

        // test divFloor(n,d)

        // test compoundInterest(rate,months,bal);

        // test fibonacci(n)
    }
    
    /* IDENTIFY ROLES
        At keyboard:
        Leading discussion:
        if 3, writing test cases in main:
    */
    public static boolean orderedUp(int x, int[] A) {
        // use a WHILE loop.
        // if every element is <= to the next one, then it is in order. Return true.
        // else return false
        return false;
    }

    /* IDENTIFY ROLES
        At keyboard:
        Leading discussion:
        if 3, writing test cases in main:
    */
    public static int max(int[] A) {
        // return the maximum valued element in the array
        return -1;
    }

    /* IDENTIFY ROLES
        At keyboard:
        Leading discussion:
        if 3, writing test cases in main:
    */    
    public static int product(int[] A) {
        // return the cumulative product of all numbers in the array
        return 0;
    }

    /* IDENTIFY ROLES
        At keyboard:
        Leading discussion:
        if 3, writing test cases in main:
    */
    public static int divFloor(int num, int den) {
        // num/den = num - den - den ... until you are almost to 0
        // another option is to use addition
        // you cannot use the divide operator of java
        // careful, one or both of the numbers can be negative
        return -1;
    }

    /* IDENTIFY ROLES
        At keyboard:
        Leading discussion:
        if 3, writing test cases in main:
    */
    public static double compoundInterest(double rate, int months, int balance) {
        // return the final balance with the given rate and duration
        // add balance*rate to current balance to get the next month's balance
        return balance;
    }

    /* IDENTIFY ROLES
        At keyboard:
        Leading discussion:
        if 3, writing test cases in main:
    */    
    public static int fibonacci(int n) {
        // return the nth fibonacci number
        // fib(0) is 0. fib(1) is 1. fib(n) = fib(n-1)+fib(n-2)
        // n must be >= 0
        return -1;
    }
}
